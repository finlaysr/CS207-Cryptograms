@startuml

' Maybe should be a interface
class Cryptogram {
    - cryptoID: int
    - cryptoType: CryptoType
    - solution: String
    - guesses: LinkedHashmap<Character, Character>
    - correctMappings: Hashmap<Character, Character>

    + **Cryptogram(CryptoID int)**

    ..setter & other funcs..
    + userGuessAccuracy(): int    
    + getHint()
    + showSolution()
    + undoInput()

    ..getters..
    + getCryptoID(): int
    + getFrequency(): Hashmap<Character, int>
}

enum CryptoType{

    + letter-to-letter
    + letter-to-number
}


note right of Cryptogram::guesses
    This is stored in a linked hashmap
    because so that we can remove the last 
    guess using the undoInput() method

    Stored in form:
    LinkedHashmap<guess, encrypted>
end note

note right of Cryptogram::correctMappings
    Stored in form:
    LinkedHashmap<correct, encrypted>
end note

note right of Cryptogram::undoInput
    pop the last value
    from the guesses stack
end note

note right of Cryptogram::undoInput #red
    User Story 3
end note

note right of Cryptogram::showSolution #red
    User Story 6
end note

note right of Cryptogram::getFrequency #red
    User Story 7
end note

note right of Cryptogram::userGuessAccuracy #red
    User Story 11
end note

note right of Cryptogram::getHint #red
    User Story 14
end note

@enduml

