@startuml

class AppData {
    - currentUser: User
    - users: ArrayList<User>
    - cryptograms: ArrayList<Cryptogram> 

    + **AppData()**
    .. setter..
    + setCurrentUser(username: String)
    + newUser(username: String)
    + checkUserExists(username: String): boolean
    + newCryptogram(): int
    ..getters..
    + getCryptogram(cryptoID: int): Cryptogram
    + getCryptograms(): ArrayList<Cryptogram> 
    + saveToFile()
    + loadFromFile()
}

note top of AppData
    Stores data while app running
end note

note right of AppData::currentUser
    Reference to one element in the users array
end note

note right of AppData::users
    <color:royalBlue>User Story 8</color>
end note

note right of AppData::newCryptogram
    <color:royalBlue>User Story 1</color>
end note

note right of AppData::saveToFile
    <color:royalBlue>User Story 4</color>
end note

note right of AppData::loadFromFile
    <color:royalBlue>User Story 5, 12</color>
end note



@enduml